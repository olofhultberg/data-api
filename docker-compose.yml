version: '3.8'

services:
    db:
        image: 'postgres:12'
        environment:
          - POSTGRES_DB=db
          - POSTGRES_USER=db
          - POSTGRES_PASSWORD=AVNS_ucogV5NGNJSzs0n
        ports:
          - '25060:25060'